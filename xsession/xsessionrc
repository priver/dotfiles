XSESSION_DIR=$HOME/.xsession.d
XSESSION_FILES=$(run-parts --list --regex '\.sh$' $XSESSION_DIR)

if [ -n "$XSESSION_FILES" ]; then
  set +e
  for XSESSION_FILE in $XSESSION_FILES; do
    . $XSESSION_FILE
  done
  set -e
fi
