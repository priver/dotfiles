[user]
	name = Mikhail Priver
	email = m.priver@gmail.com
[core]
	autocrlf = input
[push]
	default = simple
[alias]
	l = log --pretty=format:'%C(auto)%h%d %C(blue)%an:%C(reset) %s %C(green)(%cr)%C(reset)'
	lg = log --graph --pretty=format:'%C(auto)%h%d %C(blue)%an:%C(reset) %s %C(green)(%cr)%C(reset)'
[color]
	ui = auto
[color "diff"]
	meta = cyan
	frag = magenta bold
	old = red bold
	new = green bold
